name: SPX Trigger

on:
  workflow_dispatch:  # DESATIVADO

permissions:
  actions: write

jobs:
  trigger:
    name: Re-disparar collector
    runs-on: ubuntu-latest
    steps:
      - name: Desativado
        run: echo "Trigger desativado"